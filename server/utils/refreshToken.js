module.exports = app => {
  'use strict';
  const router = require('express').Router(),
    _ = require('lodash'),
    jwt = require('jsonwebtoken'),
    jwtRefresh = require('jsonwebtoken-refresh');

  return router;
};
